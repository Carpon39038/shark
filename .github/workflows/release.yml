name: Release

# Build, sign, and publish Shark installers when a version tag is pushed.
# Tag with e.g. `v0.1.0` (must start with `v`). Produces a *draft* GitHub
# Release carrying macOS (Apple Silicon + Intel) and Windows artifacts plus the
# updater `latest.json` manifest that the in-app updater points at.
#
# IMPORTANT — the draft must be PUBLISHED for auto-update to work. The updater
# endpoint is `…/releases/latest/download/latest.json`, and `/releases/latest/`
# only resolves the latest *published* (non-draft, non-prerelease) release. While
# the release stays a draft, that URL 404s and the in-app check finds nothing.
# So after this workflow finishes, review the draft and click "Publish release".
# Publishing is also the moment to confirm `latest.json` lists all platforms
# (see the `max-parallel` note below).
on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  release:
    strategy:
      fail-fast: false
      # Run the three platform jobs one at a time. tauri-action builds the
      # updater `latest.json` by reading the manifest already on the release and
      # merging in the current job's platform entry. That read-modify-write is
      # not atomic: run the jobs concurrently and two can read the same manifest
      # before either writes, so the later writer drops the earlier platform.
      # Serializing guarantees each job sees the previous job's entry, so the
      # final `latest.json` carries all three platforms.
      max-parallel: 1
      matrix:
        include:
          # Apple Silicon
          - platform: macos-latest
            target: aarch64-apple-darwin
            args: "--target aarch64-apple-darwin"
          # Intel
          - platform: macos-latest
            target: x86_64-apple-darwin
            args: "--target x86_64-apple-darwin"
          # Windows (x64, default host target)
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            args: ""

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"
          key: ${{ matrix.target }}

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build and release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Updater signing (required for the in-app updater to trust builds).
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # --- Apple code signing + notarization (placeholder) ---
          # Leave these secrets unset for now: the build still produces an
          # unsigned .app/.dmg. To enable notarized macOS builds later, add the
          # secrets in the repo settings and they will be picked up automatically.
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "Shark ${{ github.ref_name }}"
          releaseBody: "See the assets below to download and install this version."
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}
